# @ventureos/agent-runtime

**Status: NOT IMPLEMENTED. Planned for Phase 3 (Board and Approval).**

This package will house the agent execution harness: tool allowlists, cost
limits, timeout enforcement, prompt versioning, and the mock/real AI provider
adapters used by the eight voting board agents plus the Decision Synthesiser
(master spec section 11). It is scaffolded now (empty package with this
README) so Phase 3 can be added without restructuring the monorepo, per
master spec section 35 ("design the Phase 1 architecture so later phases
can be added without major rewrites").

Do not import from this package until Phase 3 lands real code here.
