# @ventureos/ui

**Status: NOT IMPLEMENTED as a shared package yet.** Phase 1 ships a minimal
set of dashboard components directly inside `apps/web/src/components` (one
consumer, no cross-app sharing needed yet). Once `apps/web` has more than one
consumer of the same components, extract them here rather than duplicating -
tracked in docs/TODO.md.
